<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fimestuds.unitrade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_unitrade"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_unitrade_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User_pssd"></activity>
        <activity android:name=".Vender_artic"
            android:label="@string/title_activity_vender_art"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Compra" />
        <activity android:name=".Pantalla_venta" />
        <activity android:name=".Perfil" />
        <activity android:name=".Registro_usu" />
        <activity android:name=".Main_page" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>